query getSupplyHistory($id: ID!, $from: Int!, $to: Int!) {
  reserves(
    where: {
      id: $id
      supplyHistory_: { timestamp_gt: $from, timestamp_lt: $to }
    }
  ) {
    id
    symbol
    supplyHistory {
      reserve {
        liquidityRate
      }
      timestamp
    }
  }
}
